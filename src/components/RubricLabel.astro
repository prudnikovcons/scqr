---
interface Props {
	name: string;
}
const { name } = Astro.props;
const hasII = name.endsWith('ии');
const base = hasII ? name.slice(0, -2) : name;
---

<span class="rubric-label"><span class="rubric-label__base">{base}</span>{hasII && <span class="rubric-label__ai">ии</span>}</span>
